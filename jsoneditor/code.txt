var json = JSON.parse(result);

var options = {
	mode: 'tree',
	error: function (err) {
		alert(err.toString());
	}
};

editorTree = new JSONEditor(document.getElementById('jsoneditorTree'), options, json);

options.mode = "code";
editorCode = new JSONEditor(document.getElementById('jsoneditorCode'), options, json);